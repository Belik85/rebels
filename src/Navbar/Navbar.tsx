import './Navbar.css'

const Navbar = () => {
    return <div className="mode">
        <span className="textleft">Where in the world?</span>
        <span className="textright" onClick={() => {
        }}>Dark mode</span>
    </div>
}

export default Navbar;